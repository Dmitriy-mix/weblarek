# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.ф

## Данные

## Модели данных

Модели данных отвечают за хранение и работу с данными в приложении. Ниже описаны три класса, реализующие эту функциональность.

### 1. Класс `ProductCatalog` (каталог товаров)

**Назначение и зона ответственности**  
Отвечает за хранение списка всех доступных товаров, а также за управление выбранным для детального просмотра товаром.

**Конструктор**  
Не принимает параметров. При создании экземпляра инициализирует пустой массив товаров и `null` для выбранного товара.

**Поля класса**

| Поле | Тип | Описание |
|------|-----|----------|
| `_products` | `IProduct[]` | Массив всех товаров, доступных в приложении |
| `_selectedProduct` | `IProduct \| null` | Товар, выбранный для подробного отображения |

**Методы класса**

| Метод | Параметры | Возвращаемое значение | Описание |
|-------|-----------|----------------------|----------|
| `setProducts(products)` | `products: IProduct[]` | `void` | Сохраняет переданный массив товаров в поле `_products` |
| `getProducts()` | нет | `IProduct[]` | Возвращает массив всех товаров |
| `getProductById(id)` | `id: string` | `IProduct \| undefined` | Возвращает товар по указанному идентификатору |
| `setSelectedProduct(product)` | `product: IProduct` | `void` | Сохраняет товар для подробного отображения |
| `getSelectedProduct()` | нет | `IProduct \| null` | Возвращает выбранный для просмотра товар |

**Интерфейс**

```typescript
interface IProduct {
  id: string;          // уникальный идентификатор товара
  description: string; // подробное описание
  image: string;       // URL изображения
  title: string;       // название товара
  category: string;    // категория (например, "электроника")
  price: number | null;// цена (null означает бесплатно или не указана)
}
```
---

### 2. Класс `Cart` (корзина)

**Назначение и зона ответственности**  
Управляет списком товаров, которые пользователь добавил для покупки. Обеспечивает добавление, удаление, подсчёт стоимости и количества товаров в корзине.

**Конструктор**  
Не принимает параметров. Инициализирует пустой массив товаров.

**Поля класса**

| Поле | Тип | Описание |
|------|-----|----------|
| `_items` | `IProduct[]` | Массив товаров, добавленных в корзину |

**Методы класса**

| Метод | Параметры | Возвращаемое значение | Описание |
|-------|-----------|----------------------|----------|
| `getItems()` | нет | `IProduct[]` | Возвращает массив товаров в корзине |
| `addItem(product)` | `product: IProduct` | `void` | Добавляет товар в корзину |
| `removeItem(productId)` | `productId: string` | `void` | Удаляет товар с указанным `id` из корзины |
| `clear()` | нет | `void` | Очищает корзину (удаляет все товары) |
| `getTotalPrice()` | нет | `number` | Возвращает сумму цен всех товаров в корзине (цены `null` трактуются как 0) |
| `getItemCount()` | нет | `number` | Возвращает количество товаров в корзине |
| `contains(productId)` | `productId: string` | `boolean` | Проверяет, присутствует ли товар с указанным `id` в корзине |

```typescript
interface ICart {
  getItems(): IProduct[];               // Получить все товары в корзине
  addItem(product: IProduct): void;     // Добавить товар
  removeItem(productId: string): void;  // Удалить товар по id
  clear(): void;                        // Очистить корзину
  getTotalPrice(): number;              // Общая стоимость товаров в корзине
  getItemCount(): number;               // Количество товаров
  contains(productId: string): boolean; // Проверить наличие товара по id
}
```
---

### 3. Класс `Buyer` (покупатель)

**Назначение и зона ответственности**  
Хранит и предоставляет доступ к данным покупателя, необходимым для оформления заказа. Выполняет валидацию полей в соответствии с функциональными требованиями.

**Конструктор**  
Не принимает параметров. Инициализирует поля значениями по умолчанию (пустые строки для текстовых полей, для `payment` - значение по умолчанию `'cash'`).

**Поля класса**

| Поле | Тип | Описание |
|------|-----|----------|
| `_payment` | `TPayment` | Выбранный способ оплаты |
| `_email` | `string` | Адрес электронной почты |
| `_phone` | `string` | Номер телефона |
| `_address` | `string` | Адрес доставки |

**Методы класса**

| Метод | Параметры | Возвращаемое значение | Описание |
|-------|-----------|----------------------|----------|
| `setPayment(payment)` | `payment: TPayment` | `void` | Устанавливает способ оплаты |
| `setEmail(email)` | `email: string` | `void` | Устанавливает email |
| `setPhone(phone)` | `phone: string` | `void` | Устанавливает номер телефона |
| `setAddress(address)` | `address: string` | `void` | Устанавливает адрес доставки |
| `setData(data)` | `data: Partial<IBuyer>` | `void` | Сохраняет одно или несколько полей покупателя (обновляет только переданные свойства) |
| `getData()` | нет | `IBuyer` | Возвращает все данные покупателя |
| `clear()` | нет | `void` | Сбрасывает все поля в значения по умолчанию |
| `validate()` | нет | `Partial<Record<keyof IBuyer, string>> \| null` | Проверяет валидность каждого поля. Поле считается валидным, если оно не пустое. Возвращает объект с ошибками или `null`, если всё валидно. |

```typescript
type TPayment = 'cash' | 'card';  // пользовательский тип (type alias), который определяет возможные способы оплаты в приложении

interface IBuyerModel {
  payment: TPayment;   // Способ оплаты: 'cash' | 'card'
  email: string;       // Адрес электронной почты
  phone: string;       // Номер телефона
  address: string;     // Адрес доставки
}
```

### Интерфейсы для обмена с сервером

#### `IOrder` – данные для отправки заказа


Используется при вызове метода `sendOrder` класса `AppApi`.


```typescript
interface IOrder {
  payment: TPayment;   // Способ оплаты: 'cash' | 'card'
  email: string;       // Электронная почта покупателя
  phone: string;       // Номер телефона
  address: string;     // Адрес доставки
  total: number;       // Итоговая сумма заказа (рассчитывается из корзины)
  items: string[];     // Массив идентификаторов товаров, которые покупает пользователь
}
```

### Интерфейс `IOrderResult`

**Назначение:** описывает структуру ответа сервера после успешного оформления заказа. Возвращается методом `sendOrder` класса `AppApi`.

```typescript
interface IOrderResult {
  id: string;    // уникальный идентификатор созданного заказа
  total: number; // итоговая сумма заказа (дублирует значение, переданное в IOrder)
}
```

## Слой представления (View)

### Абстрактный класс `Component`

**Назначение:** базовый абстрактный класс для всех компонентов пользовательского интерфейса. Предоставляет набор утилитарных методов для работы с DOM и единый интерфейс рендеринга.

**Конструктор:** `protected constructor(protected readonly container: HTMLElement)`

**Параметры конструктора:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `container` | `HTMLElement` | Корневой DOM-элемент компонента |

**Поля класса:**

| Поле | Тип | Описание |
|------|-----|----------|
| `container` | `HTMLElement` | Корневой DOM-элемент компонента (защищённое, только для чтения) |

**Защищённые методы (утилиты для работы с DOM):**

| Метод | Параметры | Описание |
|-------|-----------|----------|
| `setText` | `element: HTMLElement, value: unknown` | Устанавливает текстовое содержимое элемента. Если элемент существует, преобразует значение в строку |
| `setDisabled` | `element: HTMLElement, state: boolean` | Блокирует или разблокирует элемент (кнопку). При `state = true` добавляет атрибут `disabled` |
| `setHidden` | `element: HTMLElement` | Скрывает элемент, устанавливая `display: none` |
| `setVisible` | `element: HTMLElement` | Показывает элемент, удаляя `display: none` |
| `setImage` | `element: HTMLImageElement, src: string, alt?: string` | Устанавливает изображению src и альтернативный текст |

**Публичные методы:**

| Метод | Параметры | Возвращаемое значение | Описание |
|-------|-----------|----------------------|----------|
| `render` | `data?: Partial<T>` | `HTMLElement` | Обновляет компонент с переданными данными и возвращает корневой DOM-элемент. Данные применяются через `Object.assign` |

**Особенности реализации:**
- Класс является **абстрактным** и не может быть инстанциирован напрямую. Все компоненты представления должны наследоваться от него
- Используется **дженерик** `T`, который определяет тип данных, ожидаемых компонентом при рендеринге
- Метод `render` автоматически присваивает свойства объекта `data` текущему экземпляру класса, что позволяет использовать сеттеры для обновления DOM
- Все DOM-операции безопасно проверяют существование элементов перед манипуляцией

---

### Класс `Modal`

**Назначение:** управляет отображением модального окна. Обеспечивает открытие, закрытие и установку содержимого модального окна. Генерирует события при изменении состояния.

**Конструктор:** `constructor(container: HTMLElement, events: IEvents)`

**Параметры конструктора:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `container` | `HTMLElement` | Корневой DOM-элемент модального окна (с классом `modal`) |
| `events` | `IEvents` | Брокер событий для генерации событий |

**Поля класса:**

| Поле | Тип | Описание |
|------|-----|----------|
| `_closeButton` | `HTMLElement` | Кнопка закрытия модального окна (класс `modal__close`) |
| `_content` | `HTMLElement` | Контейнер для динамического содержимого (класс `modal__content`) |

**Свойства (геттеры/сеттеры):**

| Свойство | Тип | Описание |
|----------|-----|----------|
| `isOpen` (геттер) | `boolean` | Возвращает `true`, если модальное окно открыто (имеет класс `modal_active`) |
| `content` (сеттер) | `HTMLElement` | Устанавливает содержимое модального окна, заменяя все дочерние элементы |

**Методы:**

| Метод | Параметры | Возвращаемое значение | Описание |
|-------|-----------|----------------------|----------|
| `open()` | нет | `void` | Открывает модальное окно (добавляет класс `modal_active`) и генерирует событие `modal:open` |
| `close()` | нет | `void` | Закрывает модальное окно (удаляет класс `modal_active`), очищает содержимое и генерирует событие `modal:close` |

**Генерируемые события:**

| Событие | Когда генерируется | Данные |
|---------|-------------------|--------|
| `modal:open` | При открытии модального окна | – |
| `modal:close` | При закрытии модального окна | – |

**Особенности реализации:**
- Для поиска элементов используется `ensureElement`, гарантирующий наличие кнопки закрытия и контейнера контента
- Клик на оверлей (фон модального окна) также закрывает окно
- При закрытии содержимое модального окна полностью очищается через `innerHTML = ''`
- Модальное окно активируется/деактивируется CSS-классом `modal_active`

---

### Класс `Form`

**Назначение:** базовый класс для всех форм приложения. Обеспечивает обработку ввода данных, валидацию и управление состоянием кнопки отправки.

**Конструктор:** `constructor(container: HTMLFormElement, protected events: IEvents)`

**Параметры конструктора:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `container` | `HTMLFormElement` | DOM-элемент формы |
| `events` | `IEvents` | Брокер событий для генерации событий |

**Поля класса:**

| Поле | Тип | Описание |
|------|-----|----------|
| `_form` | `HTMLFormElement` | DOM-элемент формы |
| `_submitButton` | `HTMLButtonElement` | Кнопка отправки формы (с типом `submit`) |
| `_errors` | `HTMLElement` | Элемент для отображения ошибок валидации (класс `form__errors`) |

**Свойства (сеттеры):**

| Свойство | Тип | Описание |
|----------|-----|----------|
| `valid` | `boolean` | Управляет состоянием кнопки отправки. При `false` кнопка блокируется, при `true` — разблокируется |
| `errors` | `string` | Устанавливает текст ошибок в элементе `form__errors` |

**Методы:**

| Метод | Параметры | Возвращаемое значение | Описание |
|-------|-----------|----------------------|----------|
| `render` | `data: Partial<T> & { valid?: boolean; errors?: string }` | `HTMLElement` | Обновляет состояние формы (валидность и ошибки) и возвращает корневой элемент |

**Генерируемые события:**

| Событие | Когда генерируется | Данные |
|---------|-------------------|--------|
| `${name}:change` | При вводе данных в любое поле формы | `{ field: keyof T, value: string }` |
| `${name}:form-submit` | При отправке формы (клик на кнопку submit) | – |

> **Примечание:** `${name}` — это значение атрибута `name` у формы. Например, для формы с `name="order"` события будут `order:change` и `order:form-submit`.

**Особенности реализации:**
- Для поиска обязательных элементов используется `ensureElement`, который гарантирует наличие кнопки отправки и контейнера для ошибок
- При отправке формы стандартное поведение (перезагрузка страницы) отменяется через `e.preventDefault()`
- Событие `input` отслеживает любые изменения полей формы и генерирует событие с именем поля и новым значением
- Класс является дженериком `Form<T>`, где `T` — тип данных формы (содержит названия полей и их типы)

---

### Класс `Basket`

**Назначение:** компонент для отображения корзины с товарами. Управляет списком товаров, общей стоимостью и кнопкой оформления заказа.

**Конструктор:** `constructor(container: HTMLElement, events: IEvents)`

**Параметры конструктора:**

| Параметр | Тип | Описание |
|----------|-----|----------|
| `container` | `HTMLElement` | Корневой DOM-элемент корзины |
| `events` | `IEvents` | Брокер событий для генерации событий |

**Поля класса:**

| Поле | Тип | Описание |
|------|-----|----------|
| `_list` | `HTMLElement` | Контейнер для списка товаров (класс `basket__list`) |
| `_totalPrice` | `HTMLElement` | Элемент для отображения общей стоимости (класс `basket__price`) |
| `_button` | `HTMLButtonElement` | Кнопка оформления заказа (класс `basket__button`) |

**Свойства (сеттеры):**

| Свойство | Тип | Описание |
|----------|-----|----------|
| `items` | `HTMLElement[]` | Устанавливает список карточек товаров в корзине. Заменяет все дочерние элементы контейнера |
| `total` | `number` | Устанавливает общую стоимость заказа. Отображается в формате `{value} синапсов` |
| `disabled` | `boolean` | Блокирует/разблокирует кнопку оформления заказа |

**Генерируемые события:**

| Событие | Когда генерируется | Данные |
|---------|-------------------|--------|
| `order:submit` | При клике на кнопку оформления заказа | – |

**Особенности реализации:**

- Для поиска элементов используется `ensureElement`, гарантирующий наличие всех обязательных элементов в DOM
- При отсутствии обязательных элементов выбрасывается ошибка
- Список товаров полностью заменяется при каждом обновлении через `replaceChildren`
- Кнопка оформления блокируется, когда корзина пуста (`disabled = true`)

---

### Класс `CardBasket`

**Назначение:** компонент для отображения карточки товара в корзине. Отвечает за показ информации о товаре (название, цена, порядковый номер) и удаление товара из корзины.

**Конструктор:** `constructor(container: HTMLElement, events: IEvents, onRemove: (id: string) => void)`

**Параметры конструктора:**

| Параметр | Тип | Описание |
|----------|-----|----------|
| `container` | `HTMLElement` | Корневой DOM-элемент карточки корзины |
| `events` | `IEvents` | Брокер событий |
| `onRemove` | `(id: string) => void` | Коллбек, вызываемый при нажатии на кнопку удаления |

**Поля класса:**

| Поле | Тип | Описание |
|------|-----|----------|
| `_title` | `HTMLElement` | Элемент с названием товара (класс `card__title`) |
| `_price` | `HTMLElement` | Элемент с ценой товара (класс `card__price`) |
| `_index` | `HTMLElement` | Элемент с порядковым номером (класс `basket__item-index`) |
| `_deleteButton` | `HTMLButtonElement` | Кнопка удаления товара (класс `basket__item-delete`) |
| `_productId` | `string` | Идентификатор товара (устанавливается при рендере) |

**Свойства (сеттеры):**

| Свойство | Тип | Описание |
|----------|-----|----------|
| `product` | `IProduct` | Устанавливает информацию о товаре: ID, название, цену |
| `index` | `number` | Устанавливает порядковый номер товара в корзине |

**Методы:**

| Метод | Параметры | Возвращаемое значение | Описание |
|-------|-----------|----------------------|----------|
| `render` | `data: { product: IProduct; index: number }` | `HTMLElement` | Рендерит карточку с переданными данными и возвращает корневой элемент |

**Особенности реализации:**

- Для поиска элементов используется `ensureElement`, гарантирующий наличие всех обязательных элементов в DOM
- При отсутствии обязательных элементов выбрасывается ошибка
- Цена отображается в формате `{value} синапсов`. Если цена отсутствует (`null`), отображается текст «Бесценно»
- При клике на кнопку удаления вызывается коллбек `onRemove` с передачей ID товара
- Класс не генерирует события напрямую, а использует переданный коллбек

---

### Класс `CardCatalog`

**Назначение:** компонент для отображения карточки товара в каталоге. Наследуется от базового класса `Card`. Отвечает за отображение краткой информации о товаре и обработку клика для просмотра деталей.

**Конструктор:** `constructor(container: HTMLElement, events: IEvents, onSelect: (id: string) => void)`

**Параметры конструктора:**

| Параметр | Тип | Описание |
|----------|-----|----------|
| `container` | `HTMLElement` | Корневой DOM-элемент карточки каталога |
| `events` | `IEvents` | Брокер событий |
| `onSelect` | `(id: string) => void` | Коллбек, вызываемый при клике на карточку |

**Поля класса:**

| Поле | Тип | Описание |
|------|-----|----------|
| `_id` | `string` | Идентификатор товара (устанавливается при рендере) |

**Свойства (сеттеры):**

| Свойство | Тип | Описание |
|----------|-----|----------|
| `id` | `string` | Устанавливает идентификатор товара и добавляет атрибут `data-id` к контейнеру |

**Методы:**

| Метод | Параметры | Возвращаемое значение | Описание |
|-------|-----------|----------------------|----------|
| `render` | `data: IProduct` | `HTMLElement` | Рендерит карточку товара, устанавливая все поля (id, title, price, category, image, buttonText) и возвращает корневой элемент |

**Особенности реализации:**

- Класс наследуется от `Card<IProduct>`, что даёт все базовые возможности (отображение названия, цены, категории, изображения)
- Весь контейнер карточки является кликабельным (обработчик навешивается на `container`, а не на кнопку)
- При клике на любую область карточки вызывается коллбек `onSelect` с передачей ID товара
- Текст кнопки всегда установлен в «В корзину»
- Класс не генерирует события напрямую, а использует переданный коллбек

---

### Класс `CardPreview`

**Назначение:** компонент для отображения карточки товара в модальном окне с детальной информацией. Наследуется от базового класса `Card`. Отвечает за отображение полной информации о товаре и управление кнопкой добавления/удаления из корзины.

**Конструктор:** `constructor(container: HTMLElement, events: IEvents, onAddToBasket: (id: string) => void, onRemoveFromBasket: (id: string) => void)`

**Параметры конструктора:**

| Параметр | Тип | Описание |
|----------|-----|----------|
| `container` | `HTMLElement` | Корневой DOM-элемент карточки превью |
| `events` | `IEvents` | Брокер событий |
| `onAddToBasket` | `(id: string) => void` | Коллбек, вызываемый при добавлении товара в корзину |
| `onRemoveFromBasket` | `(id: string) => void` | Коллбек, вызываемый при удалении товара из корзины |

**Поля класса:**

| Поле | Тип | Описание |
|------|-----|----------|
| `_id` | `string` | Идентификатор товара |
| `_inBasket` | `boolean` | Флаг, указывающий, находится ли товар в корзине |
| `onAdd` | `(id: string) => void` | Коллбек для добавления товара |
| `onRemove` | `(id: string) => void` | Коллбек для удаления товара |

**Свойства (сеттеры):**

| Свойство | Тип | Описание |
|----------|-----|----------|
| `id` | `string` | Устанавливает идентификатор товара и добавляет атрибут `data-id` |
| `inBasket` | `boolean` | Устанавливает состояние корзины и меняет текст кнопки |

**Методы:**

| Метод | Параметры | Возвращаемое значение | Описание |
|-------|-----------|----------------------|----------|
| `render` | `data: IProduct` | `HTMLElement` | Рендерит карточку товара и возвращает корневой элемент |

---

### Класс `ContactsForm`

**Назначение:** компонент формы для ввода контактных данных покупателя (email и телефон). Наследуется от базового класса `Form`. Отвечает за валидацию полей и управление состоянием кнопки отправки.

**Конструктор:** `constructor(container: HTMLFormElement, events: IEvents)`

**Параметры конструктора:**

| Параметр | Тип | Описание |
|----------|-----|----------|
| `container` | `HTMLFormElement` | DOM-элемент формы |
| `events` | `IEvents` | Брокер событий |

**Поля класса:**

| Поле | Тип | Описание |
|------|-----|----------|
| `_email` | `HTMLInputElement` | Поле ввода email |
| `_phone` | `HTMLInputElement` | Поле ввода телефона |
| `_emailValue` | `string` | Хранит текущее значение email |
| `_phoneValue` | `string` | Хранит текущее значение телефона |

**Свойства (сеттеры):**

| Свойство | Тип | Описание |
|----------|-----|----------|
| `email` | `string` | Устанавливает значение поля email и обновляет валидность формы |
| `phone` | `string` | Устанавливает значение поля телефона и обновляет валидность формы |

**Генерируемые события:**

| Событие | Когда генерируется | Данные |
|---------|-------------------|--------|
| `contacts:change` | При вводе данных в поле email или phone | `{ field: 'email' \| 'phone', value: string }` |

**Особенности реализации:**

- Для поиска элементов используется `ensureElement`, гарантирующий наличие полей ввода
- Валидация формы: оба поля должны быть не пустыми (после удаления пробелов)
- Кнопка отправки разблокируется только когда заполнены оба поля
- При изменении любого поля генерируется событие `contacts:change`
- Начальное состояние кнопки — заблокировано (`valid = false`)

---

### Класс `OrderForm`

**Назначение:** компонент формы для ввода данных заказа (способ оплаты и адрес доставки). Наследуется от базового класса `Form`. Отвечает за выбор способа оплаты, ввод адреса и валидацию формы.

**Конструктор:** `constructor(container: HTMLFormElement, events: IEvents)`

**Параметры конструктора:**

| Параметр | Тип | Описание |
|----------|-----|----------|
| `container` | `HTMLFormElement` | DOM-элемент формы |
| `events` | `IEvents` | Брокер событий |

**Поля класса:**

| Поле | Тип | Описание |
|------|-----|----------|
| `_paymentOnline` | `HTMLButtonElement` | Кнопка выбора оплаты онлайн (name="card") |
| `_paymentCash` | `HTMLButtonElement` | Кнопка выбора оплаты наличными (name="cash") |
| `_address` | `HTMLInputElement` | Поле ввода адреса доставки |
| `_paymentValue` | `TPayment` | Хранит выбранный способ оплаты |

**Свойства (сеттеры):**

| Свойство | Тип | Описание |
|----------|-----|----------|
| `payment` | `TPayment` | Устанавливает выбранный способ оплаты и обновляет активный стиль кнопки |
| `address` | `string` | Устанавливает значение поля адреса |

**Генерируемые события:**

| Событие | Когда генерируется | Данные |
|---------|-------------------|--------|
| `order:change` | При выборе способа оплаты или вводе адреса | `{ field: 'payment' \| 'address', value: string }` |

**Особенности реализации:**

- Для поиска элементов используется `ensureElement`, гарантирующий наличие кнопок и поля ввода
- Валидация формы: адрес не должен быть пустым и должен быть выбран способ оплаты
- Кнопка отправки разблокируется только когда выбран способ оплаты и введён адрес
- Активная кнопка способа оплаты получает класс `button_alt-active`
- Начальное состояние кнопки — заблокировано (`valid = false`)

---

### Класс `Page`

**Назначение:** компонент управления главной страницей приложения. Отвечает за отображение каталога товаров, счётчика корзины и блокировку прокрутки при открытии модальных окон.

**Конструктор:** `constructor(container: HTMLElement, events: IEvents)`

**Параметры конструктора:**

| Параметр | Тип | Описание |
|----------|-----|----------|
| `container` | `HTMLElement` | Корневой DOM-элемент страницы |
| `events` | `IEvents` | Брокер событий |

**Поля класса:**

| Поле | Тип | Описание |
|------|-----|----------|
| `_catalog` | `HTMLElement` | Контейнер для карточек каталога (класс `gallery`) |
| `_basketCounter` | `HTMLElement` | Элемент счётчика корзины (класс `header__basket-counter`) |
| `_basket` | `HTMLElement` | Кнопка-иконка корзины (класс `header__basket`) |
| `_wrapper` | `HTMLElement` | Обёртка страницы для блокировки прокрутки (класс `page__wrapper`) |

**Свойства (сеттеры):**

| Свойство | Тип | Описание |
|----------|-----|----------|
| `catalog` | `HTMLElement[]` | Устанавливает карточки товаров в каталог, заменяя все дочерние элементы |
| `counter` | `number` | Устанавливает значение счётчика товаров в корзине |
| `locked` | `boolean` | Блокирует/разблокирует прокрутку страницы (добавляет/удаляет класс `page__wrapper_locked`) |

**Генерируемые события:**

| Событие | Когда генерируется | Данные |
|---------|-------------------|--------|
| `basket:open` | При клике на иконку корзины | – |

**Особенности реализации:**

- Для поиска элементов используется `ensureElement`, гарантирующий наличие всех обязательных элементов в DOM
- При отсутствии обязательных элементов выбрасывается ошибка
- Список карточек каталога полностью заменяется через `replaceChildren`
- Блокировка прокрутки реализована через CSS-класс на обёртке страницы
- Счётчик корзины отображает количество товаров (`counter`)

---

### Класс `Success`

**Назначение:** компонент для отображения модального окна с подтверждением успешного оформления заказа. Отображает сумму списания и предоставляет кнопку для закрытия окна.

**Конструктор:** `constructor(container: HTMLElement, events: IEvents)`

**Параметры конструктора:**

| Параметр | Тип | Описание |
|----------|-----|----------|
| `container` | `HTMLElement` | Корневой DOM-элемент окна успеха |
| `events` | `IEvents` | Брокер событий |

**Поля класса:**

| Поле | Тип | Описание |
|------|-----|----------|
| `_description` | `HTMLElement` | Элемент с описанием списания (класс `order-success__description`) |
| `_button` | `HTMLButtonElement` | Кнопка закрытия окна (класс `order-success__close`) |

**Свойства (сеттеры):**

| Свойство | Тип | Описание |
|----------|-----|----------|
| `total` | `number` | Устанавливает сумму списания в текст описания в формате «Списано {value} синапсов» |

**Генерируемые события:**

| Событие | Когда генерируется | Данные |
|---------|-------------------|--------|
| `success:close` | При клике на кнопку закрытия окна | – |

**Особенности реализации:**

- Для поиска элементов используется `ensureElement`, гарантирующий наличие всех обязательных элементов в DOM
- При отсутствии обязательных элементов выбрасывается ошибка
- Текст описания автоматически форматируется с подстановкой суммы списания
- При клике на кнопку генерируется событие `success:close`, которое обычно закрывает модальное окно и очищает корзину

---
